rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允许任何人对 data/{pin}/ 下的文档读写（PIN 即家庭码）
    match /data/{pin}/{document=**} {
      allow read, write: if true;
    }
  }
}
